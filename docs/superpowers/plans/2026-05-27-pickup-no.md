# 取餐号(外带 X号) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为外带订单生成稳定的“外带 X号”并在所有展示/打印/通知中统一呈现, 支持可配置的每日重置时间。

**Architecture:** 在订单创建时从“每日计数表”原子递增生成 pickup_no, 持久化到订单表。展示端根据 pickup_no 渲染外带文案, 堂食继续只显示桌号。重置时间存储于店铺设置, 通过纯函数计算业务日期。

**Tech Stack:** NestJS 10 + Drizzle ORM + MySQL 8, React 18 + Vite, 微信小程序原生。

---

## 文件结构与变更点

**Create:**
- `server/drizzle/0005_pickup_no.sql`
- `server/src/modules/orders/pickup-no.core.ts`
- `server/src/modules/orders/pickup-no.core.spec.ts`

**Modify:**
- `server/src/storage/database/shared/schema.ts`
- `server/src/modules/store-settings/dto/store-settings.dto.ts`
- `server/src/modules/store-settings/store-settings.service.ts`
- `server/src/modules/orders/orders.service.ts`
- `server/src/modules/notif/notification-dispatcher.service.ts`
- `server/src/modules/merchant-ops/print/print.types.ts`
- `server/src/modules/merchant-ops/print/readonly-order.drizzle.ts`
- `server/src/modules/merchant-ops/print/print.core.ts`
- `admin-web/src/pages/StoreSettings.tsx`
- `admin-web/src/pages/OrderManage.tsx`
- `admin-web/src/components/NotificationCenter.tsx`
- `xiaochengxu/pages/orders-list/orders-list.wxml`
- `xiaochengxu/pages/detail/detail.wxml`

---

### Task 1: 业务日期计算(纯函数) + TDD

**Files:**
- Create: `server/src/modules/orders/pickup-no.core.ts`
- Create: `server/src/modules/orders/pickup-no.core.spec.ts`

- [ ] **Step 1: 写失败用例**

`server/src/modules/orders/pickup-no.core.spec.ts`
```ts
import { describe, it, expect } from 'vitest';
import { computeBizDate } from './pickup-no.core';

describe('computeBizDate', () => {
  it('reset 00:00 uses same calendar date', () => {
    const d = new Date('2026-05-27T10:15:00');
    expect(computeBizDate(d, '00:00')).toBe('2026-05-27');
  });

  it('reset 04:00 treats before 04:00 as previous day', () => {
    const before = new Date('2026-05-27T03:59:00');
    const after = new Date('2026-05-27T04:00:00');
    expect(computeBizDate(before, '04:00')).toBe('2026-05-26');
    expect(computeBizDate(after, '04:00')).toBe('2026-05-27');
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd server; npm test -- pickup-no.core.spec.ts`

Expected: FAIL (computeBizDate 未实现)

- [ ] **Step 3: 实现最小功能**

`server/src/modules/orders/pickup-no.core.ts`
```ts
export function computeBizDate(now: Date, resetTime: string): string {
  const [hh, mm] = resetTime.split(':').map((v) => Number(v));
  const resetMinutes = (hh || 0) * 60 + (mm || 0);
  const curMinutes = now.getHours() * 60 + now.getMinutes();

  const d = new Date(now);
  if (curMinutes < resetMinutes) {
    d.setDate(d.getDate() - 1);
  }

  const y = d.getFullYear();
  const m = String(d.getMonth() + 1).padStart(2, '0');
  const day = String(d.getDate()).padStart(2, '0');
  return `${y}-${m}-${day}`;
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd server; npm test -- pickup-no.core.spec.ts`

Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add server/src/modules/orders/pickup-no.core.ts server/src/modules/orders/pickup-no.core.spec.ts
git commit -m "test: add pickup biz date core"
```

---

### Task 2: 数据库迁移 + Drizzle Schema

**Files:**
- Create: `server/drizzle/0005_pickup_no.sql`
- Modify: `server/src/storage/database/shared/schema.ts`

- [ ] **Step 1: 写迁移文件**

`server/drizzle/0005_pickup_no.sql`
```sql
-- 取餐号(外带 X号)支持
-- 1) orders 增加 pickup_no
-- 2) store_settings 增加 pickup_reset_time
-- 3) daily_pickup_counters 计数表

ALTER TABLE `orders` ADD COLUMN `pickup_no` int DEFAULT NULL COMMENT '取餐号(外带X号)';
ALTER TABLE `store_settings` ADD COLUMN `pickup_reset_time` varchar(5) NOT NULL DEFAULT '00:00' COMMENT '取餐号重置时间(HH:mm)';

CREATE TABLE IF NOT EXISTS `daily_pickup_counters` (
  `biz_date` date NOT NULL,
  `current_no` int NOT NULL DEFAULT 0,
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`biz_date`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

- [ ] **Step 2: 更新 Drizzle schema**

`server/src/storage/database/shared/schema.ts`
```ts
export const orders = mysqlTable(
  "orders",
  {
    id: int("id").autoincrement().primaryKey(),
    table_id: int("table_id").notNull().references(() => tables.id),
    order_number: varchar("order_number", { length: 50 }).notNull().unique(),
    total_amount: decimal("total_amount", { precision: 10, scale: 2 }).notNull().default('0'),
    status: varchar("status", { length: 20 }).notNull().default('submitted'),
    order_type: varchar("order_type", { length: 20 }).notNull().default('dine_in'),
    pickup_no: int("pickup_no"),
    user_id: int("user_id").references(() => users.id),
    remark: varchar("remark", { length: 500 }),
    printed_at: timestamp("printed_at"),
    settled_at: timestamp("settled_at"),
    created_at: timestamp("created_at").defaultNow().notNull(),
    updated_at: timestamp("updated_at").defaultNow().notNull(),
  },
  (table) => [
    index("orders_table_id_idx").on(table.table_id),
    index("orders_order_number_idx").on(table.order_number),
    index("orders_status_idx").on(table.status),
    index("orders_user_id_idx").on(table.user_id),
    index("orders_created_at_idx").on(table.created_at),
  ]
);

export const store_settings = mysqlTable(
  "store_settings",
  {
    id: int("id").autoincrement().primaryKey(),
    store_name: varchar("store_name", { length: 100 }).notNull().default('我的小店'),
    store_avatar: varchar("store_avatar", { length: 500 }),
    smtp_mode: varchar("smtp_mode", { length: 20 }).notNull().default('platform'),
    smtp_host: varchar("smtp_host", { length: 255 }),
    smtp_port: int("smtp_port"),
    smtp_user: varchar("smtp_user", { length: 255 }),
    smtp_pass_enc: varchar("smtp_pass_enc", { length: 512 }),
    smtp_from: varchar("smtp_from", { length: 255 }),
    smtp_secure: boolean("smtp_secure").notNull().default(true),
    pickup_reset_time: varchar("pickup_reset_time", { length: 5 }).notNull().default('00:00'),
    created_at: timestamp("created_at").defaultNow().notNull(),
    updated_at: timestamp("updated_at").defaultNow().notNull(),
  }
);

export const daily_pickup_counters = mysqlTable(
  "daily_pickup_counters",
  {
    biz_date: varchar("biz_date", { length: 10 }).primaryKey(),
    current_no: int("current_no").notNull().default(0),
    updated_at: timestamp("updated_at").defaultNow().notNull(),
  }
);
```

- [ ] **Step 3: 提交**

```bash
git add server/drizzle/0005_pickup_no.sql server/src/storage/database/shared/schema.ts
git commit -m "chore: add pickup no schema"
```

---

### Task 3: 店铺设置支持取餐号重置时间

**Files:**
- Modify: `server/src/modules/store-settings/dto/store-settings.dto.ts`
- Modify: `server/src/modules/store-settings/store-settings.service.ts`

- [ ] **Step 1: DTO 增加 pickup_reset_time 校验**

`server/src/modules/store-settings/dto/store-settings.dto.ts`
```ts
import { IsString, IsOptional, MaxLength, Matches } from 'class-validator';

const NO_XSS = /^[^<>]*$/;
const HHMM = /^([01]\d|2[0-3]):[0-5]\d$/;

export class UpdateStoreSettingsDto {
  @IsString()
  @MaxLength(100)
  @Matches(NO_XSS)
  store_name: string;

  @IsOptional()
  @IsString()
  @MaxLength(512)
  store_avatar?: string;

  @IsOptional()
  @IsString()
  @Matches(HHMM)
  pickup_reset_time?: string;
}
```

- [ ] **Step 2: service 返回/更新 pickup_reset_time**

`server/src/modules/store-settings/store-settings.service.ts`
```ts
  async getStoreSettings() {
    const settings = await db
      .select({
        id: store_settings.id,
        store_name: store_settings.store_name,
        store_avatar: store_settings.store_avatar,
        pickup_reset_time: store_settings.pickup_reset_time,
      })
      .from(store_settings)
      .limit(1);

    if (settings.length === 0) {
      return { store_name: '我的小店', store_avatar: '', pickup_reset_time: '00:00' };
    }
    return settings[0];
  }

  async updateStoreSettings(data: { store_name: string; store_avatar?: string; pickup_reset_time?: string }) {
    const existing = await db.select().from(store_settings).limit(1);

    if (existing.length === 0) {
      await db.insert(store_settings).values({
        store_name: data.store_name,
        store_avatar: data.store_avatar || null,
        pickup_reset_time: data.pickup_reset_time || '00:00',
      });
      return { store_name: data.store_name, store_avatar: data.store_avatar || '', pickup_reset_time: data.pickup_reset_time || '00:00' };
    }

    const oldAvatar = existing[0].store_avatar ?? null;
    const nextAvatar = data.store_avatar !== undefined ? data.store_avatar : oldAvatar;
    const nextReset = data.pickup_reset_time !== undefined ? data.pickup_reset_time : existing[0].pickup_reset_time || '00:00';

    await db.update(store_settings)
      .set({
        store_name: data.store_name,
        store_avatar: nextAvatar,
        pickup_reset_time: nextReset,
      })
      .where(eq(store_settings.id, existing[0].id));

    if (oldAvatar && data.store_avatar && oldAvatar !== data.store_avatar) {
      void this.imageCleanup.removeByUrl(oldAvatar);
    }

    return { store_name: data.store_name, store_avatar: nextAvatar, pickup_reset_time: nextReset };
  }
```

- [ ] **Step 3: 提交**

```bash
git add server/src/modules/store-settings/dto/store-settings.dto.ts server/src/modules/store-settings/store-settings.service.ts
git commit -m "feat: add pickup reset time settings"
```

---

### Task 4: 订单创建时分配 pickup_no

**Files:**
- Modify: `server/src/modules/orders/orders.service.ts`

- [ ] **Step 1: 增加分配方法(使用原子递增)**

`server/src/modules/orders/orders.service.ts`
```ts
import { sql } from 'drizzle-orm';
import { StoreSettingsService } from '../store-settings/store-settings.service';
import { computeBizDate } from './pickup-no.core';

@Injectable()
export class OrdersService {
  constructor(
    private readonly ordersGateway: OrdersGateway,
    private readonly notifDispatcher: NotificationDispatcherService,
    private readonly storeSettingsService: StoreSettingsService,
  ) {}

  private async allocatePickupNo(now: Date): Promise<number> {
    const settings = await this.storeSettingsService.getStoreSettings();
    const reset = settings?.pickup_reset_time || '00:00';
    const bizDate = computeBizDate(now, reset);

    return await db.transaction(async (tx) => {
      await tx.execute(sql`
        INSERT INTO daily_pickup_counters (biz_date, current_no)
        VALUES (${bizDate}, 1)
        ON DUPLICATE KEY UPDATE current_no = LAST_INSERT_ID(current_no + 1)
      `);
      const rows = await tx.execute(sql`SELECT LAST_INSERT_ID() AS current_no`);
      const value = (rows as any)[0]?.current_no ?? (rows as any)[0]?.[0]?.current_no;
      if (!value) throw new Error('pickup_no allocate failed');
      return Number(value);
    });
  }
```

- [ ] **Step 2: createOrder 写入 pickup_no**

`server/src/modules/orders/orders.service.ts`
```ts
    const orderNumber = this.generateOrderNumber();
    const now = new Date();
    const pickupNo = await this.allocatePickupNo(now);

    const insertResult = await db.insert(orders).values({
      table_id: finalTableId,
      order_number: orderNumber,
      total_amount: totalAmount.toFixed(2),
      user_id: dto.user_id,
      remark: dto.remark,
      status: 'submitted',
      order_type: isTakeaway ? 'takeaway' : 'dine_in',
      pickup_no: pickupNo,
    });
```

- [ ] **Step 3: 提交**

```bash
git add server/src/modules/orders/orders.service.ts
git commit -m "feat: allocate pickup no on order create"
```

---

### Task 5: 通知 tableLabel 显示外带号

**Files:**
- Modify: `server/src/modules/notif/notification-dispatcher.service.ts`
- Modify: `admin-web/src/components/NotificationCenter.tsx`

- [ ] **Step 1: 后端通知上下文 tableLabel**

`server/src/modules/notif/notification-dispatcher.service.ts`
```ts
    const isTakeaway =
      order.order_type === 'takeaway' || table?.table_number === '__TAKEAWAY__';
    const pickupNo = order.pickup_no as number | null | undefined;
    const tableLabel = isTakeaway
      ? (pickupNo ? `外带 ${pickupNo}号` : '外带')
      : `${table?.table_number || '?'} 号桌`;
```

- [ ] **Step 2: 管理后台通知中心显示**

`admin-web/src/components/NotificationCenter.tsx`
```tsx
        const isTakeaway = data.order_type === 'takeaway'
        const tableNum = data.tables?.table_number || data.table_id
        const pickupNo = data.pickup_no
        const tableLabel = isTakeaway
          ? (pickupNo ? `🛍️ 外带 ${pickupNo}号` : '🛍️ 外带')
          : `${tableNum}号桌`
```

- [ ] **Step 3: 提交**

```bash
git add server/src/modules/notif/notification-dispatcher.service.ts admin-web/src/components/NotificationCenter.tsx
git commit -m "feat: show pickup no in notifications"
```

---

### Task 6: 打印小票显示外带号

**Files:**
- Modify: `server/src/modules/merchant-ops/print/print.types.ts`
- Modify: `server/src/modules/merchant-ops/print/readonly-order.drizzle.ts`
- Modify: `server/src/modules/merchant-ops/print/print.core.ts`

- [ ] **Step 1: 扩展类型**

`server/src/modules/merchant-ops/print/print.types.ts`
```ts
export interface PrintPayload {
  width: PrintWidth;
  fields: TemplateField[];
  store_name?: string;
  table_number?: string;
  order_type?: string;
  pickup_no?: number;
  order_no?: string;
  order_time?: string;
  items?: Array<{ name: string; quantity: number; spec?: string | null; subtotal?: number }>;
  total?: number;
  remark?: string | null;
  operator?: string | null;
}

export interface OrderProjection {
  order_id: number;
  order_no: string;
  table_number: string;
  order_type: string;
  pickup_no?: number;
  store_name: string;
  created_at: Date;
  total_amount: number;
  remark: string | null;
  operator: string | null;
  items: Array<{
    order_item_id: number;
    category_id: number;
    name: string;
    spec: string | null;
    quantity: number;
    subtotal: number;
    phase: 'order' | 'add_more';
    add_more_round: number;
  }>;
}
```

- [ ] **Step 2: 读订单投影带 pickup_no**

`server/src/modules/merchant-ops/print/readonly-order.drizzle.ts`
```ts
      .select({
        id: orders.id,
        order_number: orders.order_number,
        order_type: orders.order_type,
        pickup_no: orders.pickup_no,
        total_amount: orders.total_amount,
        remark: orders.remark,
        created_at: orders.created_at,
        table_number: tables.table_number,
      })
```

```ts
    return {
      order_id: h.id,
      order_no: h.order_number,
      table_number: h.table_number ?? '-',
      order_type: h.order_type || 'dine_in',
      pickup_no: (h as any).pickup_no ?? undefined,
      store_name: storeName,
      created_at: h.created_at as Date,
      total_amount: Number(h.total_amount ?? 0),
      remark: h.remark ?? null,
      operator: null,
      items: ...
    };
```

- [ ] **Step 3: 打印文案显示外带号**

`server/src/modules/merchant-ops/print/print.core.ts`
```ts
function formatTableLabel(payload: PrintPayload): { label: string; value: string } {
  const isTakeaway = payload.order_type === 'takeaway' || payload.table_number === TAKEAWAY_TABLE_SENTINEL;
  if (isTakeaway) {
    const no = payload.pickup_no;
    return { label: '类型', value: no ? `🛍️ 外带 ${no}号` : '🛍️ 外带' };
  }
  return { label: '桌号', value: payload.table_number ?? '-' };
}
```

```ts
    const payload: PrintPayload = {
      width: tpl.width,
      fields,
      store_name: order.store_name,
      table_number: order.table_number,
      order_type: order.order_type,
      pickup_no: order.pickup_no,
      order_no: ...,
      ...
    };
```

- [ ] **Step 4: 提交**

```bash
git add server/src/modules/merchant-ops/print/print.types.ts server/src/modules/merchant-ops/print/readonly-order.drizzle.ts server/src/modules/merchant-ops/print/print.core.ts
git commit -m "feat: show pickup no in print"
```

---

### Task 7: 管理后台 UI

**Files:**
- Modify: `admin-web/src/pages/StoreSettings.tsx`
- Modify: `admin-web/src/pages/OrderManage.tsx`

- [ ] **Step 1: StoreSettings 增加取餐号重置时间**

`admin-web/src/pages/StoreSettings.tsx`
```tsx
  const [pickupResetTime, setPickupResetTime] = useState('00:00');

  const fetchSettings = async () => {
    ...
    if (data) {
      setStoreName(data.store_name || '');
      ...
      setPickupResetTime(data.pickup_reset_time || '00:00');
    }
  };

  const handleSubmit = async () => {
    if (!storeName.trim()) { ... }
    await request.put('/store-settings', {
      store_name: storeName.trim(),
      store_avatar: storeAvatar,
      pickup_reset_time: pickupResetTime || '00:00',
    });
  };
```

```tsx
          <div>
            <label className="block text-sm font-medium text-[#334155] mb-2">取餐号重置时间</label>
            <input
              type="time"
              className="w-full h-10 px-3 border border-[#E2E8F0] rounded-lg text-sm text-[#0F172A] outline-none focus:border-[#2563EB] transition-colors"
              value={pickupResetTime}
              onChange={(e) => setPickupResetTime(e.target.value)}
            />
            <p className="text-xs text-[#94A3B8] mt-1">默认 00:00, 例如 04:00 表示凌晨 4 点重置</p>
          </div>
```

- [ ] **Step 2: OrderManage 外带标签显示外带号**

`admin-web/src/pages/OrderManage.tsx`
```tsx
const formatTakeawayLabel = (order: Order) => {
  const no = order.pickup_no
  return no ? `外带 ${no}号` : '外带'
}
```

```tsx
{isTakeawayOrder(order) ? (
  <span className="inline-flex items-center gap-1.5 px-3 py-1.5 rounded-lg bg-purple-100 text-purple-700 text-sm font-bold border border-purple-300">
    <ShoppingBag size={16} /> {formatTakeawayLabel(order)}
  </span>
) : (
  <div className="text-xl font-bold text-[#2563EB]">{order.tables?.table_number || '-'}</div>
)}
```

(同文件内平板/移动/详情弹窗等其它“外带”展示点同样替换为 `formatTakeawayLabel(order)`)

- [ ] **Step 3: 提交**

```bash
git add admin-web/src/pages/StoreSettings.tsx admin-web/src/pages/OrderManage.tsx
git commit -m "feat: add pickup reset time ui"
```

---

### Task 8: 小程序 UI

**Files:**
- Modify: `xiaochengxu/pages/orders-list/orders-list.wxml`
- Modify: `xiaochengxu/pages/detail/detail.wxml`

- [ ] **Step 1: 订单列表外带标签**

`xiaochengxu/pages/orders-list/orders-list.wxml`
```xml
<block wx:if="{{item.order_type === 'takeaway'}}">
  <text class="om-takeaway-tag">🛍 外带 {{item.pickup_no ? item.pickup_no + '号' : ''}}</text>
</block>
```

- [ ] **Step 2: 订单详情外带标签**

`xiaochengxu/pages/detail/detail.wxml`
```xml
<block wx:if="{{order.order_type === 'takeaway'}}">
  <text class="om-meta-takeaway">🛍 外带 {{order.pickup_no ? order.pickup_no + '号' : ''}}</text>
</block>
```

- [ ] **Step 3: 提交**

```bash
git add xiaochengxu/pages/orders-list/orders-list.wxml xiaochengxu/pages/detail/detail.wxml
git commit -m "style: show pickup no in miniprogram"
```

---

### Task 9: 端到端校验

- [ ] **Step 1: 运行后端测试**

Run: `cd server; npm test -- pickup-no.core.spec.ts`

Expected: PASS

- [ ] **Step 2: 手工验证点**
- 新建外带订单 -> 显示“外带 X号”(小程序/后台/通知/小票)
- 新建堂食订单 -> 只显示桌号
- 改重置时间 -> 次日(或跨时刻)序号归 1
- 旧订单 -> 不显示“外带 -号”

---

## Self-Review

- **Spec 覆盖:**
  - 生成时机/重置时间/断号/展示范围/堂食不显示/旧订单回退均已覆盖。
- **占位符检查:** 无 TBD/TODO。
- **类型一致性:** pickup_no 在 orders/schema/print/notification/UI 统一为 number | undefined。

---

Plan complete and saved to `docs/superpowers/plans/2026-05-27-pickup-no.md`. Two execution options:

1. **Subagent-Driven (recommended)** - I dispatch a fresh subagent per task, review between tasks, fast iteration
2. **Inline Execution** - Execute tasks in this session using executing-plans, batch execution with checkpoints

Which approach?

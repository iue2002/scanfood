# 小程序开发版访问后端配置指南

## 问题说明

微信小程序开发版/体验版无法访问 `http://localhost:3000` 这类本地地址，需要使用公网可访问的 HTTPS 服务。

## 推荐方案：使用内网穿透工具

### 方案 A：cpolar（推荐，免费）

1. **下载安装**
   - 访问 https://www.cpolar.com/
   - 下载 Windows 版本并安装

2. **启动穿透**
   ```bash
   # 在命令行中运行
   cpolar http 3000
   ```

3. **获取公网地址**
   - 启动后会看到类似：`https://xxxxxx.cpolar.cn -> localhost:3000`
   - 复制这个 HTTPS 地址

4. **修改小程序配置**
   - 编辑 `xiaochengxu/config.js`
   - 将 `SERVER_URL` 修改为 cpolar 提供的地址：
   ```javascript
   SERVER_URL: 'https://xxxxxx.cpolar.cn',  // 替换为您的 cpolar 地址
   ```

### 方案 B：NATAPP（免费）

1. **注册账号**
   - 访问 https://natapp.cn/ 注册

2. **购买免费隧道**
   - 登录后购买免费隧道
   - 配置本地端口为 3000

3. **下载并运行**
   - 下载对应系统的客户端
   - 运行：`natapp -authtoken=您的authtoken`

4. **修改小程序配置**
   - 同上，将获取到的公网地址填入配置

### 方案 C：ngrok（需要注册）

1. **下载安装**
   - 访问 https://ngrok.com/ 下载

2. **注册账号获取 authtoken**

3. **运行**
   ```bash
   ngrok http 3000
   ```

## 微信小程序配置

### 1. 配置服务器域名（体验版/正式版必需）

登录微信公众平台：
- 进入「开发」->「开发管理」->「开发设置」
- 在「服务器域名」中配置：
  - request合法域名：填入您的公网 HTTPS 地址
  - uploadFile合法域名：同上
  - downloadFile合法域名：同上

### 2. 开发工具临时调试（仅开发者工具）

在微信开发者工具中：
- 点击右上角「详情」
- 选择「本地设置」
- 勾选「不校验合法域名、web-view（业务域名）、TLS版本以及HTTPS证书」

## 工作流程

1. **启动后端服务**
   ```bash
   cd server
   npm run dev
   ```

2. **启动内网穿透**
   ```bash
   cpolar http 3000
   # 或
   natapp -authtoken=xxx
   ```

3. **更新小程序配置**
   - 将获取到的公网地址填入 `xiaochengxu/config.js`

4. **重新编译小程序**
   - 在微信开发者工具中重新编译

## 注意事项

1. **免费工具的限制**
   - 免费版地址通常会变化，每次重启需要更新配置
   - 网速可能有限制

2. **HTTPS 要求**
   - 微信小程序必须使用 HTTPS（除了开发工具临时调试）
   - 内网穿透工具通常提供 HTTPS 地址

3. **正式上线**
   - 正式发布时需要使用有固定域名的服务器
   - 配置好 SSL 证书

## 快速测试

配置好后，在小程序中测试：
- 打开小程序
- 尝试获取数据
- 查看网络请求是否成功

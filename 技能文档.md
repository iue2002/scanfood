# 已安装 Skill 命令大全

> 项目共安装 **37 个 skill**，按来源分组如下。

---

## 📦 Vercel 官方

### `find-skills`
Skill 搜索引擎。想找什么 skill 直接搜：
> "find a skill for X" / "is there a skill that can..."

### `vercel-react-best-practices`
React/Next.js 生产级性能优化规则（45+ 条）。写 React 代码时自动生效。

---

## 📦 mattpocock/skills（TypeScript 大神）

### `grill-me`
写代码前先被追问需求，对齐理解。适合启动新功能前使用。

### `grill-with-docs`
进阶版 grilling，还会同步更新项目文档（CONTEXT.md / ADR）。

### `tdd`
强制 Red-Green-Refactor TDD 循环，测试先行。

### `diagnose`
结构化 debug：复现 → 最小化 → 假设 → 探测 → 修复 → 回归测试。

### `improve-codebase-architecture`
分析代码库架构，寻找重构机会，消除紧耦合。

### `prototype`
快速搭建可抛弃的原型，验证设计思路。

### `zoom-out`
让 AI 从更高视角解释代码在整体架构中的定位。

### `to-prd`
把当前对话上下文转为 PRD 文档。

### `to-issues`
把 PRD 或计划拆解为可单独领取的 Issue。

### `triage`
Issue 分类排期，状态机驱动的工作流管理。

### `handoff`
把当前会话压缩成交接文档，给另一个 agent 接手。

### `write-a-skill`
创建新的自定义 skill。

### `setup-matt-pocock-skills`
首次使用前运行，配置 Issue 跟踪器、标签体系等。

---

## 📦 superpowers（obra）— 完整开发方法论

### `using-superpowers`
入口 skill。每次对话自动检查可用的 superpowers skill。

### `brainstorming`
苏格拉底式设计讨论。先想清楚需求、设计、权衡再动手。

### `writing-plans`
把需求拆解为详细的实现计划（2-5 分钟粒度的任务）。

### `executing-plans`
按计划执行批量任务，含人工检查点。

### `test-driven-development`
Superpowers 版的 TDD cycle。

### `systematic-debugging`
4 阶段根因排查法。

### `verification-before-completion`
完成工作前自动运行验证，通过后才能声称"做完了"。

### `dispatching-parallel-agents`
2 个以上独立任务时，派发子 agent 并行执行。

### `subagent-driven-development`
用子 agent 执行实现计划中的独立任务。

### `using-git-worktrees`
用 git worktree 隔离功能分支开发环境。

### `requesting-code-review`
完成任务/合码前发起 Code Review。

### `receiving-code-review`
收到 Review 反馈后的处理流程。

### `finishing-a-development-branch`
功能完成后决定 merge / 提 PR / 清理。

### `writing-skills`
创建、编辑、验证新的 skill。

---

## 📦 caveman（JuliusBrussee）— 极致省 Token

### `caveman`
核心技能。让 AI 输出压缩成"山顶洞人"风格，省 **65-75% token**。
> 例： "New object ref each render. Inline object prop = new ref = re-render. Wrap in `useMemo`."

### `cavecrew`
Caveman 风格的多人协作模式。

### `caveman-commit`
用压缩风格写 commit message。

### `caveman-compress`
把 CLAUDE.md 等文件压缩约 45%。

### `caveman-help`
Caveman 帮助文档入口。

### `caveman-review`
用 caveman 风格做 Code Review。

### `caveman-stats`
统计节省了多少 token。

---

## 📦 Karpathy 行为准则

### `karpathy-guidelines`
Andrej Karpathy 总结的 LLM 编码陷阱防范指南。四个原则：
1. **Think Before Coding** — 先思考再动手
2. **Simplicity First** — 简洁优先
3. **Surgical Changes** — 精准修改，不碰无关代码
4. **Goal-Driven Execution** — 定义验证标准再执行

---

## 💡 使用方式

在对话中直接输入 skill 名称即可调用，例如：

```
/grill-me
/tdd
/diagnose
/caveman
/find-skills
```

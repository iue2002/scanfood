# 需求文档

## 1. 应用概述

### 1.1 应用名称
单门店桌码点餐系统

### 1.2 应用描述
本系统包含用户端微信小程序和商家后台管理Web系统两部分。用户通过扫描桌码进入点餐界面,支持同桌多人协作点餐,订单提交后自动触发小票打印。商家通过独立Web后台管理桌台、订单、菜品,并手动标记结账状态完成交易闭环。后端数据库使用Supabase。

## 2. 用户与使用场景

### 2.1 目标用户
- 用户端:到店就餐的顾客
- 商家端:餐厅管理人员、收银员

### 2.2 核心使用场景
- 顾客扫描桌台二维码进入点餐界面,选择菜品下单
- 同桌多人同时操作点餐,订单实时同步
- 顾客提交订单后,系统自动打印小票
- 商家通过Web后台管理桌台状态、订单、菜品
- 顾客用餐结束后,商家前台手动标记结账状态

## 3. 页面结构与功能说明

### 3.1 用户端小程序页面结构

```
用户端小程序
├── 登录页面
├── 点餐页面
│   ├── 菜品列表区
│   ├── 购物车区
│   └── 订单提交区
└── 订单详情页面
```

### 3.2 商家后台Web系统页面结构

```
商家后台管理系统(Web)
├── 登录页面
├── 桌台管理页面
├── 订单管理页面
├── 菜品管理页面
├── 支付/退款/售后管理页面
└── 数据统计页面
```

### 3.3 用户端小程序功能说明

-#### 3.3.1 登录弹窗
- 仅支持微信授权登录(一键登录)，
- 微信登录后默认自动建档，必要时提示用户补全手机号等信息（可选完善）
- 登录流程应兼容小程序无网络或网络切换场景的容错体验

#### 3.3.2 点餐页面
使用标准的微信小程序二维码生成工具进行生成桌台二维码
- 用户通过扫描桌台二维码进入对应桌台的点餐界面
- 菜品列表区:展示所有可点菜品,包含菜品名称、价格、图片
- 菜品操作:
  - 选择菜品(辣度)
  - 调整菜品数量(增加/减少)
  - 添加菜品到购物车
- 购物车区:
  - 展示当前桌台所有已选菜品及数量
  - 支持同桌多人同时点餐,购物车实时共享与同步（WebSocket 驱动）
  - 支持补单、加餐、减餐操作
- 订单提交区:
  - 显示订单总金额
  - 提交订单按钮
  - 订单提交后自动触发小票打印功能

#### 3.3.3 订单详情页面
- 展示当前桌台订单详情,包括菜品名称、份量、数量、单价、总价
- 显示订单状态(已提交、已打印、已结账等)

#### 3.3.4 实时同步与草稿订单
- 支持桌台级别的“草稿订单/购物车”（draft）：扫码进入桌台后获取或创建草稿，草稿在多人之间实时共享
- 实时同步通道：使用 WebSocket（网关/gateways），事件包括但不限于 `draft:update`, `order:submitted`, `order:printed`, `order:status_change`, `table:status_change`, `dishes:changed`
- 客户端订阅粒度：按 `table_id` 和 `order_id` 订阅事件；收到事件后采用事件触发的轻量重拉或增量更新策略
- 草稿生命周期：draft -> submitted -> printed -> settled/cancelled/refunded；提交时草稿转为正式订单并触发打印
- 推荐数据模型：复用 `orders` 表并增加 `status=draft` 而非单独创建新表，以减少迁移复杂度，但需明确是否纳入统计与打印边界
- 并发策略：后台应保证草稿/订单的乐观或悲观并发控制，避免重复提交或丢失更新

### 3.4 商家后台Web系统功能说明

#### 3.4.1 登录页面
- 商家管理员通过账号密码登录后台系统

#### 3.4.2 桌台管理页面
- 展示所有桌台列表,包含桌台编号、状态(空闲、已占用、已结账)
- 支持设置桌台状态
- 支持自定义配置桌台信息(如桌台编号、容纳人数)

#### 3.4.3 订单管理页面
- 记录所有桌台订单详情,包括桌台编号、菜品明细、订单金额、订单状态、下单时间
- 支持查看订单详情
- 支持修改订单(调整菜品数量、删除菜品)
- 支持取消订单
- 支持处理补单、加餐、减餐操作
- 支持手动标记结账状态(无需接入真实支付流程)

#### 3.4.4 菜品管理页面
- 展示所有菜品列表,包含菜品名称、价格、规格、上下架状态
- 支持新增菜品
- 支持编辑菜品信息(名称、价格、规格、图片)
- 支持菜品上下架操作
- 支持删除菜品

#### 3.4.5 支付/退款/售后管理页面
- 提供手动结账入口,商家前台收取现金后手动标记订单为已结账状态
- 支持退款操作,更新订单状态为已退款
- 支持售后订单管理,记录售后原因及处理结果

#### 3.4.6 数据统计页面
- 按菜品分类统计:
  - 展示各菜品分类的销售数量
  - 展示各菜品分类的销售金额
- 按日统计:
  - 展示每日营业额
  - 展示每日订单数
  - 展示每日菜品销售排行
- 按月统计:
  - 展示每月营业额
  - 展示每月订单数
  - 展示每月菜品销售排行
- 财报打印小票功能:
  - 支持将日报统计数据打印成小票格式输出
  - 支持将月报统计数据打印成小票格式输出

## 4. 业务规则与逻辑

-### 4.1 扫码点餐流程
- 用户扫描桌台二维码后,系统识别桌台编号,进入对应桌台点餐界面
- 同一桌台多人扫码进入后,共享同一桌台草稿订单(购物车)，操作通过 WebSocket 实时同步

### 4.2 订单同步规则
- 同桌多人点餐时,任一用户添加/修改菜品,其他用户界面实时更新
- 订单提交后,所有用户界面显示订单已提交状态
### 4.2 订单同步规则
- 实时同步优先方案: 使用 WebSocket 双向通道,服务端广播订单/草稿/桌台/菜品变更事件
- 客户端在接收到 `draft:update` 等事件后,按需增量更新购物车界面或触发轻量重拉以保证一致性
- 当草稿被提交为正式订单时,服务端触发 `order:submitted` 事件并通知所有订阅该 `table_id` 的客户端
- 对已提交订单的加餐/补单应生成新的小票或按照业务规则追加打印

### 4.3 小票打印规则
- 用户提交订单后,系统自动触发小票打印功能
- 小票内容包含:桌台编号、菜品明细、数量、单价、总价、下单时间

### 4.4 补单/加餐/减餐规则
- 补单/加餐:用户在已提交订单后,可继续添加菜品并提交,系统生成新的小票
- 减餐:用户在订单提交前可减少菜品数量,提交后需通过商家后台处理

### 4.5 结账流程
- 用户用餐结束后,商家前台通过后台系统手动标记订单为已结账状态
- 结账后,桌台状态更新为已结账或空闲

### 4.6 数据存储规则
- 后端数据库使用Supabase存储桌台信息、订单数据、菜品数据、用户数据

### 4.7 数据统计规则
- 按菜品分类统计基于已结账订单数据计算
- 按日统计基于当日已结账订单数据计算
- 按月统计基于当月已结账订单数据计算
- 菜品销售排行按销售数量降序排列
- 财报小票内容包含统计时间范围、营业额、订单数、菜品销售排行

## 5. 异常与边界情况

| 场景 | 处理方式 |
|------|----------|
| 用户扫描无效二维码 | 提示「二维码无效,请扫描正确的桌台二维码」 |
| 同桌多人同时提交订单 | 以最后一次提交为准,其他提交操作提示「订单已提交」 |
| 订单提交后小票打印失败 | 记录打印失败状态,商家后台可重新触发打印 |
| 商家修改已提交订单 | 修改后生成新的小票,原小票标记为已作废 |
| 商家取消订单 | 订单状态更新为已取消,桌台状态恢复为空闲 |
| 菜品下架后订单中仍包含该菜品 | 已提交订单不受影响,新订单无法选择已下架菜品 |
| 统计时间范围内无已结账订单 | 显示营业额为0,订单数为0,菜品销售排行为空 |
| 财报打印小票失败 | 记录打印失败状态,支持重新触发打印 |

## 6. 验收标准

1. 用户扫描桌台二维码,成功进入对应桌台点餐界面
2. 用户选择菜品、调整份量和数量,添加到购物车
3. 同桌另一用户扫码进入,看到购物车中已有菜品,继续添加新菜品
4. 任一用户提交订单,系统自动打印小票,所有用户界面显示订单已提交
5. 商家通过Web后台查看订单详情,确认菜品明细正确
6. 用户用餐结束后,商家前台在后台系统手动标记订单为已结账状态
7. 桌台状态更新为已结账或空闲
8. 商家进入数据统计页面,查看按日统计的营业额、订单数、菜品销售排行
9. 商家点击财报打印按钮,系统打印日报小票

## 7. 本期不实现功能

- 真实支付流程(微信支付、支付宝支付等)
- 会员系统与积分功能
- 优惠券与促销活动
- 菜品评价与评分功能
- 多门店管理功能
- 库存管理功能
- 预约订座功能
- 外卖配送功能

## 8. 实施计划

Plan: 小程序点餐修复与实时同步
目标是修复小程序菜品读取、移除后台式入口、优化微信登录注册体验，并让订单/桌台/菜品状态与后台实时一致；同时支持同桌多人共享购物车、不同桌台并发点餐。方案是：修正接口响应契约与图片 URL，建立“桌台购物车/草稿订单”机制 + WebSocket 推送，前端按桌台/订单订阅事件并即时刷新。

实施步骤:
1. 定位“菜品读取不到”的根因：核对 `/api/dishes` 返回结构与小程序读取逻辑是否一致，并验证图片 URL 是否能被小程序访问；小程序端读取逻辑兼容当前返回结构。
2. 收敛小程序入口：移除账号密码登录 UI 与逻辑，仅保留微信一键登录；清理后台管理式文案或入口。
3. 登录注册体验：微信一键登录 + 可选手机号完善，默认自动建档；仅在需要时提示补全资料。
4. 引入桌台共享购物车/草稿订单：扫码进入桌台后获取或创建“草稿”，多人同桌共享加/减/清空；提交时转为正式订单并触发打印。
5. 建立 WebSocket 推送通道：订单创建/加餐/减餐/结账/取消、桌台状态变化、菜品上下架时广播；前端按 `table_id`/`order_id` 订阅并增量刷新或轻量重拉（与步骤4 并行）。
6. 统一订单/桌台状态机与草稿边界：明确 `draft → submitted → printed → settled/cancelled/refunded`；结账后清理桌台草稿，避免并发写不一致。
7. 端到端联调：同桌多人实时共享购物车、不同桌台并发点餐、后台改状态实时推送、小程序与后台一致。

验证项:
- 小程序端：扫码进入桌台后能读取菜品、图片显示正常。
- 同桌多人点餐：A/B 两台手机加入同一桌台，购物车实时同步并最终提交为同一订单。
- 多桌并发：不同桌台同时点餐互不影响，后台看板与小程序一致。
- 实时推送：后台改订单状态后，小程序订单详情与后台看板即时更新。

## 9. 决策与备注

- 登录/注册：仅保留微信一键登录，手机号为可选补全字段。
- 实时同步：采用 WebSocket 双向通道为首选方案，以保证低延迟的多人协作体验。
- 草稿模型：推荐复用 `orders` 表并使用 `status=draft`，减少新表带来的复杂度，但需明确统计与打印边界。
- 客户端刷新策略：事件触发轻量重拉优先于复杂的增量合并，以降低客户端合并逻辑复杂度。
- 若确认本方案，我方可按上述步骤开始实施并提交具体代码/接口变更 PR。